# AFM.ai Web (Next.js)

The Next.js frontend lives at the **repository root** (`app/`, `components/`).

- Run: `npm run dev:web` or `npm run dev` (starts API server too)
- Never call OpenAI/Gemini keys from the browser — use `/api/*` gateway routes only.
